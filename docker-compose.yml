services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: kovio
      POSTGRES_PASSWORD: kovio
      POSTGRES_DB: kovio
    ports:
      - "5432:5432"
    volumes:
      - kovio_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kovio -d kovio"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      KOVIO_DATABASE_URL: postgresql+asyncpg://kovio:kovio@db:5432/kovio
      KOVIO_DEV_AUTO_CREATE_TABLES: "true"
      KOVIO_ENVIRONMENT: dev
      KOVIO_KEY_PEPPER: kovio-dev-pepper-change-in-prod
      KOVIO_SUPABASE_JWT_SECRET: local-test-secret
      # Verify real Supabase (ES256) session JWTs against the project's JWKS so a
      # browser login on localhost works against this local API. HS256 secret
      # above stays as a fallback for synthetic test tokens.
      KOVIO_SUPABASE_URL: https://acughqaekwknfowlntcl.supabase.co

volumes:
  kovio_pgdata:
